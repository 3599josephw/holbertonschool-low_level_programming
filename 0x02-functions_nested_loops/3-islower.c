#include "main.h"
/**
 * _islower - inception babey
 * @c: the char being checked
 * Return: On success 1.
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
	{
	return (1);
	}
else
	{
	return (0);
	}
}
