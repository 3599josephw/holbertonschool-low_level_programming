#include "main.h"
/**
 * *_memset - function
 * @s: variable
 * @b: variable
 * @n: variable
 * Return: the replaced array
 */
char *_memset(char *s, char b, unsigned int n)
{

	unsigned int i;

	i = 0;
	while (i < n)
	{
		s[i] = b;
		i++;
	}
	return (s);

}
